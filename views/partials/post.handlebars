<div class="container">
    <div class="card">
        <a href="/post/{{id}}" id="title">{{title}}</a>
        <h5>Posted by {{user.username}} on {{ format_date createdAt}}</h5>
        <br>
        
        <p>{{body}}</p>
        
    </div>
</div>
