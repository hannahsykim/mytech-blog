
    <div class="card">
        <a href='/post/{{id}}' style="color: black;">{{title}}</a>
        <h5>Posted by {{username}} on {{createdAt}}</h5>
        <br>
        
        <p>{{body}}</p>
        
    </div>

